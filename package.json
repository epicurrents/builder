{
    "name": "epicurrents-app",
    "version": "0.1.0",
    "description": "Epicurrents application",
    "main": "index.js",
    "scripts": {
        "build": "npm run build:assets && npm run build:ohif:prod && npm run copy:all && cd interface && npm run build:app",
        "build:app": "cd interface && npm run build:app",
        "build:assets": "node scripts/build.js util,interface,epicurrents",
        "build:ohif:dev": "cd ohif && cross-env PUBLIC_URL=/ohif/ yarn run build && cd .. && node scripts/convert.js ohif && npm run copy:ohif",
        "build:ohif:prod": "cross-env NODE_ENV=production APP_CONFIG=config/nextcloud.js PUBLIC_URL=__NEXTCLOUD_BASE_PATH__/apps/epicurrents/viewer/ NX_SKIP_NX_CACHE=true yarn run build",
        "clean": "node scripts/clean.js epicurrents",
        "copy:all": "node scripts/copy.js",
        "copy:ohif": "node scripts/copy.js ohif",
        "copy:workers": "node scripts/copy.js workers",
        "instl": "node scripts/install.js",
        "setup": "node scripts/setup.js",
        "start": "npm run copy:workers && cd interface && npm run dev",
        "update": "node scripts/update.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "Sampsa Lohi",
    "license": "Apache-2.0",
    "workspaces": [
        "util/*",
        "epicurrents/*",
        "interface"
    ],
    "devDependencies": {
        "cross-env": "^7.0.3"
    }
}
